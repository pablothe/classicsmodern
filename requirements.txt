### requirements.txt ###

# Core dependencies
python-dotenv
requests
beautifulsoup4

# Local TTS (Kokoro) - 100% local, Apache 2.0, 52 voices
# Models auto-download to ~/.cache/kokoro/ (~335MB, one-time)
kokoro-tts>=2.3.0
kokoro-onnx>=0.3.9
soundfile>=0.13.1

# Note: Also requires FFmpeg for audio post-processing
# macOS: brew install ffmpeg
# Linux: sudo apt install ffmpeg

# Web Player
flask
uvicorn  # For FastAPI server
fastapi  # API server

# AI Assistant (Local LLM via Ollama)
ollama>=0.1.0
sentence-transformers>=2.2.0  # Semantic search for AI chat RAG

# EPUB conversion (optional, for epub_to_md.py)
# NOTE: ebooklib is AGPL-licensed and NOT included here.
# Install separately if needed: pip install ebooklib
markdownify>=0.11.0

# External LLM APIs (optional, for OpenAI/Anthropic support)
# Uncomment to enable:
# openai>=1.0.0
# anthropic>=0.18.0
