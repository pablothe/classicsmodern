### requirements.txt ###

# Core dependencies
openai
python-dotenv
requests
beautifulsoup4

# Local TTS (XTTS-v2) - Optional but recommended
# Uncomment to use local_tts_xtts.py for $0 cost audiobooks
TTS==0.27.3

# Note: Also requires FFmpeg for audio post-processing
# macOS: brew install ffmpeg
# Linux: sudo apt install ffmpeg

# Web Player
flask